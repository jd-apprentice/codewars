// https://www.codewars.com/kata/56676e8fabd2d1ff3000000c

function findNeedle(haystack) {
  const index = haystack.indexOf('needle')
  const response = `found the needle at position ${index}`
  return response
}
